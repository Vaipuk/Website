import '../components/V4/v4-tokens.css';

export default function V4Page() {
  return (
    <div
      data-theme="v4"
      style={{
        background: 'var(--v4-bg)',
        color: 'var(--v4-ink)',
        fontFamily: 'var(--v4-display)',
        minHeight: '100dvh',
        width: '100%',
      }}
    >
      {/* sections added here as each task completes */}
    </div>
  );
}
